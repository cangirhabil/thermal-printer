# NPM yapılandırması
# Sabit versiyonlar için otomatik ^ ve ~ kullanımını kapat
save-exact=true

# Legacy peer dependencies kullan (uyumluluk için)
legacy-peer-deps=false

# Otomatik güncelleme kontrolünü devre dışı bırak
update-notifier=false
